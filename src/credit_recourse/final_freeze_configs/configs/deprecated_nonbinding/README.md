# Deprecated non-binding configs

`final_candidate_library_calibrated.NONBINDING.yaml` is quarantined and must not be used by active Stage2-6 code. The only active candidate-library source is `../final_candidate_library.yaml` plus Stage2-generated quantile-specific copies under `data/final_freeze/configs/`.
